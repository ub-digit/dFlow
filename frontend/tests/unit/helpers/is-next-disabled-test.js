
import { isNextDisabled } from 'd-flow-ember/helpers/is-next-disabled';
import { module, test } from 'qunit';

module('Unit | Helper | is next disabled');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isNextDisabled([42]);
  assert.ok(result);
});

