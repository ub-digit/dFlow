
import { isPreviousDisabled } from 'd-flow-ember/helpers/is-previous-disabled';
import { module, test } from 'qunit';

module('Unit | Helper | is previous disabled');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isPreviousDisabled([42]);
  assert.ok(result);
});

