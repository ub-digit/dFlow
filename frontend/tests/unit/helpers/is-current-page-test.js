
import { isCurrentPage } from 'd-flow-ember/helpers/is-current-page';
import { module, test } from 'qunit';

module('Unit | Helper | is current page');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isCurrentPage([42]);
  assert.ok(result);
});

